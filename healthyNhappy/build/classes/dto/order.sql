CREATE TABLE ORDER_PRD
(
    ORDER_SEQ NUMBER(8) PRIMARY KEY,
    ORDER_ID VARCHAR2(50),
    ORDER_REQUEST VARCHAR2(500) NOT NULL,
    ORDER_DATE DATE NOT NULL,
    ORDER_ADDRESS VARCHAR2(50) NOT NULL,
    ORDER_PRICE NUMBER(10) NOT NULL
    ); 

ALTER TABLE ORDER_PRD
ADD CONSTRAINT FK_ORDER_ID FOREIGN KEY(ORDER_ID)
REFERENCES MEMBER(ID);   
  
ALTER TABLE ORDER_PRD
ADD CONSTRAINT FK_ORDER_SEQ FOREIGN KEY(ORDER_PRD_SEQ)
REFERENCES PRODUCT(PRD_SEQ); 

CREATE SEQUENCE SEQ_ORDER
START WITH 1
INCREMENT BY 1;

ALTER TABLE ORDER_PRD ADD ORDER_DEL NUMBER(8) DEFAULT 0 NOT NULL;

SELECT * FROM ORDER_PRD;
